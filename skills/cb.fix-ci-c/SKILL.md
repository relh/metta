---
name: cb.fix-ci-c
description: 'Use when fixing CI.'
---

# Fix Ci C

## Trigger

- Primary: "ci as needed again"
- Variant: "ci comment"
- Variant: "ci comments"
- Variant: "ci etc"
- Variant: "branch if ci fails or comments appear"
- Variant: "ci issues lint issues"
- Variant: "ci recipes so that this works"

## Workflow

- Reproduce the issue in a focused way (failing test, lint error, traceback, or CI failure).
- Apply the smallest code or test change that resolves the issue.
- Verify with targeted checks first, then broader checks if needed.
- Summarize root cause and the concrete fix made.
