---
name: t.test-them-failing-lint
description: 'Use when testing them failing lint.'
---

# Test Them Failing Lint

## Trigger

- Primary: "them for if they re failing lint"
- Variant: "them out one at a time fetch origin merge origin"
- Variant: "me we dont need to stack them they are standalone"

## Workflow

- Identify the exact behavior that needs validation.
- Run the smallest relevant test command first.
- If failures appear, isolate root cause before broad reruns.
- Report pass/fail status and any follow-up risks.
