---
name: t.fix-tests-role-distribution-match
description: 'Use when fixing tests role distribution match.'
---

# Fix Tests Role Distribution Match

## Trigger

- Primary: "tests update role distribution tests to match current defaults 35e53e9"
- Variant: "tests update role distribution tests to match current defaults"
- Variant: "tests update tests for adaptive flavor defaults cg 1i7o 442a024"
- Variant: "tests update all test files to use nlankyagentsmultipolicy instead of"
- Variant: "tests use brutal difficulty in patrol tests for determini stic"
- Variant: "tests update te lines ctrl o to expand let me"
- Variant: "tests use newtestcontroller in scout tests for deterministic behavior author"

## Workflow

- Reproduce the issue in a focused way (failing test, lint error, traceback, or CI failure).
- Apply the smallest code or test change that resolves the issue.
- Verify with targeted checks first, then broader checks if needed.
- Summarize root cause and the concrete fix made.
