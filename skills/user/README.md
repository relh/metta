# User skills

Use this folder for user-specific skills or templates that should live alongside the shared `skills/` registry without
becoming global defaults.

Layout

- `skills/user/<username>/` for user-specific skill material.
- If you add skills in this area, keep the same `SKILL.md` format as global skills so tools can discover them
  consistently.
