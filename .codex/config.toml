# Project-scoped Codex configuration.
# User-level overrides live in ~/.codex/config.toml.

# Track skill usage in Datadog (mirrors .claude/hooks/track-skill-usage.sh).
notify = ["bash", ".codex/hooks/track-skill-usage.sh"]
