# Richard Higgins - Workspace-agnostic slash commands

This document is now consolidated into the canonical skills registry.

## Canonical skills

Use the shared skills list in `docs/ai/skills.md` and the definitions under `skills/<name>/SKILL.md`.

## Notes

- The former command list mapped 1:1 to skills and is intentionally removed to avoid duplication.
- If you need to add or adjust a command, update the appropriate skill in `skills/`.
